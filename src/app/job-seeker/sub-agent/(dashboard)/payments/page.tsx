
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function SubAgentPaymentsPage() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Payments</CardTitle>
            </CardHeader>
            <CardContent>
                <p>A table to track payments from candidates, upload receipts, and view verification status will be here.</p>
            </CardContent>
        </Card>
    );
}
