
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function AgentSettingsPage() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Settings</CardTitle>
            </CardHeader>
            <CardContent>
                <p>Manage your notification preferences, team options, compliance rules, and branding from this page.</p>
            </CardContent>
        </Card>
    );
}
