
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function AdminPipelinesPage() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Pipelines</CardTitle>
            </CardHeader>
            <CardContent>
                <p>A comprehensive view of all candidates in the hiring pipeline across all jobs will be available here.</p>
            </CardContent>
        </Card>
    );
}
