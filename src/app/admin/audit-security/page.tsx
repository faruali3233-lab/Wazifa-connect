
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function AdminAuditSecurityPage() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Audit & Security</CardTitle>
            </CardHeader>
            <CardContent>
                <p>Audit logs, session management, and permissions control will be managed from this page.</p>
            </CardContent>
        </Card>
    );
}
