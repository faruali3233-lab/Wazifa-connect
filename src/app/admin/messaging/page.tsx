
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function AdminMessagingPage() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Messaging Moderation & Broadcast</CardTitle>
            </CardHeader>
            <CardContent>
                <p>Tools for moderating user messages and sending platform-wide broadcasts will be available here.</p>
            </CardContent>
        </Card>
    );
}
